import { publicAssetUrl } from "../../lib/publicAssetUrl";

/** Figma Logos 451:3976 — «Розроблено в» + OPENTECH + softserve (165×20). */
export const POWERED_BY_LOGOS_SRC = publicAssetUrl("/showcase/footer-powered-by.svg");

export const POWERED_BY_LOGOS_ALT =
  "Розроблено в OPENTECH та SoftServe";

/** Same asset path for product pages with Sub-page Hero (alias for clarity). */
export const SUBPAGE_HERO_POWERED_BY_SRC = publicAssetUrl(
  "/showcase/subpage-hero-powered-by.svg",
);
