import type { ComponentPropsWithoutRef, ReactNode } from "react";

/** Figma SubTag (3:7422) — Property 1: Default · Hover (Variant2 у Figma) */
export type SubTagProps = ComponentPropsWithoutRef<"span"> & {
  children: ReactNode;
};
