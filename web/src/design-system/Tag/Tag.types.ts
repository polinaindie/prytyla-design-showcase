import type { ComponentPropsWithoutRef, ReactNode } from "react";

/** Figma Tag (3:7429) — Property 1: Default · Hover (CSS) */
export type TagProps = ComponentPropsWithoutRef<"span"> & {
  children: ReactNode;
};
