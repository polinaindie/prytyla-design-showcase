/** Figma Tooltip (1318:54224) — category breadcrumb on hover. */
export type TooltipProps = {
  /** Segments joined with « → » (e.g. parent categories). */
  segments: readonly string[];
  id?: string;
  className?: string;
};
